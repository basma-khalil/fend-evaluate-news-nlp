import 'core-js/stable';
import 'regenerator-runtime/runtime';
import {showNav} from '../js/app';


describe('Test showNav() should be defined' , () => {
    it('Should be defined', async () => {
        expect(showNav).toBeDefined();
    });
});